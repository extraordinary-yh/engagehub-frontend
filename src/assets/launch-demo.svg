<svg xmlns="http://www.w3.org/2000/svg" width="720" height="160" viewBox="0 0 720 160" role="img" aria-label="Launch Demo">
  <defs>
    <linearGradient id="g" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#FF416C"/>
      <stop offset="100%" stop-color="#FF4B2B"/>
    </linearGradient>
    <filter id="shadow" x="-20%" y="-20%" width="140%" height="140%">
      <feDropShadow dx="0" dy="10" stdDeviation="12" flood-color="rgba(255,75,43,0.45)"/>
    </filter>
  </defs>

  <!-- Button background -->
  <rect x="10" y="10" rx="24" ry="24" width="700" height="140" fill="url(#g)" filter="url(#shadow)"/>
  <!-- subtle border for dark mode -->
  <rect x="10" y="10" rx="24" ry="24" width="700" height="140" fill="none" stroke="rgba(255,255,255,0.22)" stroke-width="2"/>

  <!-- Emoji -->
  <text x="210" y="98" font-size="56" text-anchor="end">🚀</text>

  <!-- Label -->
  <text x="240" y="100" font-family="Inter,Segoe UI,Arial,sans-serif"
        font-size="46" font-weight="800" fill="#FFFFFF" letter-spacing="1.5">
    Launch Demo →
  </text>
</svg>
